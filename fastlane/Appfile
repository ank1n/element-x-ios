# sTalk iOS — Fastlane configuration
# Update TEAM_ID after Apple Developer enrollment completes

app_identifier("ru.implica.stalk")
apple_id("dp.bondar@gmail.com")
team_id("PLACEHOLDER_TEAM_ID") # Update with actual Team ID from Apple Developer account

for_platform :ios do
  app_identifier("ru.implica.stalk")
end
