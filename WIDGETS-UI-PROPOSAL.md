# Предложение по UI для виджетов и записи

## Текущая структура Element X

### HomeScreen (список комнат)
```
┌─────────────────────────────────────┐
│ [Avatar]  Chats          [+]        │ ← Toolbar
├─────────────────────────────────────┤
│ 🔍 Search                           │
├─────────────────────────────────────┤
│ 💬 Room 1                           │
│ 💬 Room 2                           │
│ 💬 Room 3                           │
└─────────────────────────────────────┘
```

### RoomScreen (экран комнаты)
```
┌─────────────────────────────────────┐
│ [Room Avatar] Room Name   [📞]      │ ← Toolbar (callButton)
├─────────────────────────────────────┤
│                                     │
│   Timeline messages...              │
│                                     │
├─────────────────────────────────────┤
│ [Composer]                     [⬆] │
└─────────────────────────────────────┘
```

---

## Вариант 1: Кнопка виджетов в RoomScreen toolbar

**Где**: В toolbar RoomScreen, рядом с кнопкой звонка

```swift
┌─────────────────────────────────────┐
│ [Avatar] Room Name  [📊] [📞]       │ ← Добавить кнопку виджетов
├─────────────────────────────────────┤
```

**Преимущества**:
- ✅ Виджеты доступны сразу в контексте комнаты
- ✅ Логичное размещение рядом с кнопкой звонка
- ✅ Не загромождает HomeScreen

**Недостатки**:
- ❌ Кнопка видна только внутри комнаты
- ❌ Пользователь должен сначала зайти в комнату

**Реализация**:
```swift
// RoomScreen.swift toolbar
ToolbarItem(placement: .primaryAction) {
    HStack(spacing: 8) {
        // Кнопка виджетов (только если есть виджеты в комнате)
        if context.viewState.hasWidgets {
            Button {
                context.send(viewAction: .showWidgets)
            } label: {
                CompoundIcon(\.chart)  // или \.apps
            }
            .accessibilityLabel("Widgets")
        }

        // Существующая кнопка звонка
        if context.viewState.shouldShowCallButton {
            callButton
        }
    }
}
```

---

## Вариант 2: Вкладка виджетов в RoomDetails

**Где**: В RoomDetails (открывается по клику на название комнаты)

```
RoomScreen → Tap Room Header → RoomDetails
                                ├─ Members
                                ├─ Polls
                                ├─ Files & media
                                └─ Widgets ← НОВАЯ ВКЛАДКА
```

**Преимущества**:
- ✅ Не загромождает toolbar
- ✅ Естественное место для "настроек комнаты"
- ✅ Виджеты рядом с другими room features

**Недостатки**:
- ❌ Требует 2 клика (Room Header → Widgets)
- ❌ Менее заметно

---

## Вариант 3: Новая вкладка в нижнем TabBar (глобальный доступ)

**Где**: Добавить вкладку "Виджеты" в главный TabBar

```
┌─────────────────────────────────────┐
│                                     │
│    Главный экран                    │
│                                     │
├─────────────────────────────────────┤
│ [🏠] [💬] [📊] [👤]                  │ ← Добавить вкладку Widgets
└─────────────────────────────────────┘
    Home Chats Widgets Profile
```

**Преимущества**:
- ✅ Виджеты доступны с любого экрана одним тапом
- ✅ Максимальная видимость
- ✅ Можно показать виджеты из всех комнат

**Недостатки**:
- ❌ Сильно меняет UI Element X
- ❌ Занимает место в TabBar
- ❌ Менее контекстуально (виджеты обычно привязаны к комнате)

---

## Вариант 4 (РЕКОМЕНДУЕМЫЙ): Баннер + кнопка в toolbar

**Где**:
1. Баннер под toolbar RoomScreen (как pinnedItemsBanner)
2. Кнопка в toolbar для быстрого доступа

```
┌─────────────────────────────────────┐
│ [Avatar] Room Name  [📊] [📞]       │ ← Кнопка в toolbar
├─────────────────────────────────────┤
│ 📊 Stats Widget available  [View]  │ ← Баннер (если есть виджет)
├─────────────────────────────────────┤
│                                     │
│   Timeline messages...              │
```

**Преимущества**:
- ✅ Заметно и доступно
- ✅ Баннер можно скрыть после первого просмотра
- ✅ Кнопка в toolbar всегда доступна
- ✅ Использует существующий паттерн (pinnedItemsBanner)

**Недостатки**:
- ❌ Занимает вертикальное пространство (но только если есть виджет)

**Реализация**:
```swift
// RoomScreen.swift
.topBanner(widgetsBanner, isVisible: context.viewState.hasWidgets && !context.viewState.widgetsBannerDismissed)
.topBanner(pinnedItemsBanner, isVisible: context.viewState.shouldShowPinnedEventsBanner)

@ViewBuilder
private var widgetsBanner: some View {
    WidgetsBannerView(
        widgetName: context.viewState.primaryWidget?.name ?? "Widget",
        onDismiss: { context.send(viewAction: .dismissWidgetsBanner) },
        onOpen: { context.send(viewAction: .showWidgets) }
    )
}

// Кнопка в toolbar
if context.viewState.hasWidgets {
    Button {
        context.send(viewAction: .showWidgets)
    } label: {
        CompoundIcon(\.chart)
    }
}
```

---

## Запись звонков - UI

### Кнопка записи в Call Screen

```
┌─────────────────────────────────────┐
│                                     │
│     [Participant 1]  [Participant 2]│
│                                     │
├─────────────────────────────────────┤
│  [🔇]  [📹]  [⏺]  [⏹]  [☎️]         │ ← Call controls
│        Mute  Video Rec  End         │
└─────────────────────────────────────┘
```

**Функциональность**:
1. Кнопка "⏺" для начала записи
2. При клике → показать consent диалог:
   ```
   ┌─────────────────────────────────┐
   │ Record this call?               │
   │                                 │
   │ All participants will be        │
   │ notified that recording has     │
   │ started.                        │
   │                                 │
   │ [Cancel]  [Start Recording]     │
   └─────────────────────────────────┘
   ```
3. После подтверждения:
   - Кнопка становится красной "🔴" (recording indicator)
   - Баннер "Recording in progress" вверху экрана
   - Все участники видят уведомление
4. Клик на "🔴" → остановить запись:
   ```
   ┌─────────────────────────────────┐
   │ Stop recording?                 │
   │                                 │
   │ [Cancel]  [Stop Recording]      │
   └─────────────────────────────────┘
   ```

---

## Приоритет реализации

### Этап 1: Recording API (Backend)
1. Создать Node.js сервис для управления Egress
2. Задеплоить в Kubernetes namespace `livekit`
3. Настроить Ingress: `api.market.implica.ru/recording/*`

### Этап 2: Запись звонков (iOS)
1. Добавить `RecordingService.swift`
2. Добавить кнопку записи в Element Call UI
3. Добавить consent диалоги
4. Добавить индикатор "Recording in progress"

### Этап 3: Виджеты (iOS) - Вариант 4
1. Создать `WidgetService.swift` для парсинга room state
2. Создать `WidgetsBannerView.swift` (как pinnedItemsBanner)
3. Добавить кнопку виджетов в RoomScreen toolbar
4. Создать `WidgetListScreen.swift` для списка виджетов
5. Создать `WidgetWebViewScreen.swift` для отображения

---

## Макет финального UI

```
RoomScreen с виджетом и звонком:

┌─────────────────────────────────────┐
│ [Avatar] Room Name  [📊] [📞]       │ ← Кнопки виджетов и звонка
├─────────────────────────────────────┤
│ 📊 Stats Widget  [View] [×]         │ ← Баннер виджета
├─────────────────────────────────────┤
│ Alice: Hello!                       │
│ Bob: Hi there                       │
│                                     │
│ [Join call]  ← Если звонок активен │
│                                     │
├─────────────────────────────────────┤
│ Type a message...            [⬆]   │
└─────────────────────────────────────┘

Call Screen с записью:

┌─────────────────────────────────────┐
│ 🔴 Recording in progress            │ ← Баннер записи
├─────────────────────────────────────┤
│                                     │
│   [Alice video]    [Bob video]      │
│                                     │
├─────────────────────────────────────┤
│   [🔇] [📹] [🔴] [☎️]               │ ← Recording active (red)
└─────────────────────────────────────┘
```

---

## Файлы для создания

### Виджеты
```
ios/ElementX/Sources/
├── Screens/RoomScreen/Widgets/
│   ├── WidgetsBannerView.swift
│   ├── WidgetListScreen.swift
│   ├── WidgetListViewModel.swift
│   ├── WidgetWebViewScreen.swift
│   └── WidgetWebViewModel.swift
├── Services/Widget/
│   ├── WidgetService.swift
│   └── WidgetModels.swift
```

### Запись
```
ios/ElementX/Sources/
├── Services/Recording/
│   ├── RecordingService.swift
│   ├── RecordingModels.swift
│   └── RecordingAPIClient.swift
├── Screens/CallScreen/
│   └── RecordingButton.swift
```

---

## Следующие шаги

1. ✅ **Утвердить UI дизайн** (Вариант 4 рекомендуется)
2. ⬜ **Создать Recording API сервис** (Node.js)
3. ⬜ **Реализовать запись звонков** (iOS)
4. ⬜ **Реализовать виджеты** (iOS)
5. ⬜ **Портировать на Android**
