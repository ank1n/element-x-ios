# 🛠️ Пошаговая инструкция Clean Build в Xcode

## 🎯 Цель
Пересобрать проект ElementX с вкладкой Widgets с полной очисткой кеша.

---

## ✅ Подтверждено через код

**Все изменения уже в коде**:
- ✅ Строка 23: `enum HomeTab: Hashable { case chats, spaces, widgets }`
- ✅ Строка 109: widgets добавлен в `setTabs()`
- ✅ 9 новых файлов добавлены в проект
- ✅ Иконки: `.extensions` / `.extensionsSolid`

**Проблема**: Xcode использует старый кеш без изменений.

---

## 📋 Шаги выполнения (5 минут)

### Шаг 1: Остановить все процессы (10 сек)
В Xcode:
1. Если приложение запущено → нажмите **⏹️ Stop** (или `⌘.`)
2. Закройте все открытые вкладки симулятора

### Шаг 2: Очистить кеш SPM (20 сек)
В **терминале** выполните:
```bash
cd /Users/ankin/Documents/element-x-fork/ios

# Очистить все кеши
rm -rf ~/Library/Developer/Xcode/DerivedData/ElementX-*
rm -rf ~/Library/Caches/org.swift.swiftpm
rm -rf ~/Library/Developer/Xcode/DerivedData/ElementX-*/SourcePackages

echo "✅ Кеши очищены"
```

### Шаг 3: Очистить Build Folder в Xcode (30 сек)
В Xcode:
1. Убедитесь что открыт проект **ElementX.xcodeproj**
2. Меню: **Product → Clean Build Folder**
3. Или нажмите: **⇧⌘K** (Shift + Command + K)
4. Дождитесь сообщения "Clean Finished"

### Шаг 4: Закрыть и переоткрыть Xcode (30 сек)
1. Закройте Xcode полностью: **⌘Q**
2. Подождите 5 секунд
3. Откройте заново:
   ```bash
   open ElementX.xcodeproj
   ```
4. Дождитесь загрузки проекта

### Шаг 5: Resolve Package Dependencies (2 минуты)
В Xcode:
1. Меню: **File → Packages → Resolve Package Versions**
2. Дождитесь завершения (индикатор в панели активностей)
3. Должно появиться сообщение "Package Resolution Complete"

**ВАЖНО**: Этот шаг скачивает ~500MB XCFrameworks (MatrixSDK, Sentry, MapLibre). Может занять 1-3 минуты.

### Шаг 6: Собрать проект (2-3 минуты)
В Xcode:
1. Выберите схему: **ElementX**
2. Выберите симулятор: **iPhone 17 Pro** (или любой iPhone)
3. Меню: **Product → Build**
4. Или нажмите: **⌘B**
5. Дождитесь сообщения **"Build Succeeded"**

### Шаг 7: Запустить приложение (30 сек)
В Xcode:
1. Меню: **Product → Run**
2. Или нажмите: **⌘R**
3. Приложение запустится на симуляторе

### Шаг 8: Проверить вкладку Widgets (10 сек)
В симуляторе:
1. Если показывается AppLock → нажмите **"Пропустить 🙈"**
2. Или войдите в аккаунт Matrix
3. Посмотрите на **нижнюю часть экрана** (TabBar)
4. Должны увидеть **4 вкладки**:
   ```
   [💬 Chats]  [🏠 Spaces]  [🧩 Widgets]  [👤 Profile]
   ```

### Шаг 9: Открыть вкладку Widgets (5 сек)
1. Нажмите на третью вкладку: **🧩 Widgets**
2. Должен открыться список комнат с виджетами
3. Нажмите на любую комнату
4. Откроется WebView с виджетом

---

## ✅ Ожидаемый результат

После всех шагов вы увидите:

```
┌──────────────────────────────────────┐
│         Виджеты           [⚙️]       │
│                                      │
│ [🏠] Room Name              [›]      │
│     1 виджет(ов)                     │
│     📱 Статистика                    │
│                                      │
│ [👤] Another Room           [›]      │
│     1 виджет(ов)                     │
│     📱 Статистика                    │
├──────────────────────────────────────┤
│ [💬]  [🏠]  [🧩]  [👤]              │
│Chats Spaces Widgets Profile          │
└──────────────────────────────────────┘
```

---

## 🐛 Если не работает

### Проблема 1: "Build Failed" с ошибками XCFramework
**Причина**: SPM не скачал зависимости

**Решение**:
1. File → Packages → Reset Package Caches
2. File → Packages → Resolve Package Versions
3. Подождите 3-5 минут
4. Product → Build

### Проблема 2: Вкладка все равно не появилась
**Причина**: Проект использует старый Binary

**Решение**:
1. Product → Clean Build Folder (⇧⌘K)
2. Удалите приложение из симулятора:
   - Долгий тап на иконку ElementX
   - Remove App → Delete App
3. Product → Run (⌘R)

### Проблема 3: "Cannot find WidgetsTabFlowCoordinator"
**Причина**: Файлы не добавлены в проект

**Решение**:
```bash
cd /Users/ankin/Documents/element-x-fork/ios
ruby add_widgets_files.rb
```

Затем в Xcode:
- Product → Clean Build Folder
- Product → Build

---

## 📊 Проверка через терминал

Если хотите проверить что код правильный:

```bash
cd /Users/ankin/Documents/element-x-fork/ios

# Проверить HomeTab enum
grep "enum HomeTab" ElementX/Sources/FlowCoordinators/UserSessionFlowCoordinator.swift

# Должно вывести:
# enum HomeTab: Hashable { case chats, spaces, widgets }

# Проверить setTabs
grep -A3 "navigationTabCoordinator.setTabs" ElementX/Sources/FlowCoordinators/UserSessionFlowCoordinator.swift

# Должно вывести widgets в массиве

# Проверить файлы в проекте
grep -c "WidgetsTabFlowCoordinator\|WidgetsListScreen\|WidgetWebViewScreen" ElementX.xcodeproj/project.pbxproj

# Должно вывести > 10
```

---

## 🎉 Успех!

Если вы видите вкладку **🧩 Widgets** в TabBar - поздравляю! Реализация работает.

**Следующий шаг**: Сделайте скриншот и покажите что всё работает! 📸

```bash
# Скриншот симулятора
xcrun simctl io booted screenshot ~/Desktop/widgets-tab-success.png
```
